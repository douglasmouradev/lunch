# aaPanel → Site lunch.tdesksolutions.com.br → Configuração → Nginx
# Cole DENTRO do bloco server { ... } (antes do location ~ \.php)

location ^~ /assets/ {
    alias /www/wwwroot/lunch.tdesksolutions.com.br/public/assets/;
    expires 7d;
    access_log off;
}
